# Compatible requirements for Python 3.12
# Original requirements.txt has numpy==1.17.0 which is incompatible
# These versions are compatible with Python 3.12 and modern packages

easydict>=1.9
numpy>=1.26.0  # Updated from 1.17.0
torch>=1.9.0  # If using PyTorch
torchvision>=0.10.0  # If using PyTorch
opencv-python>=4.5.0  # Usually already installed
Pillow>=10.0.0  # Usually already installed

# Note: You may not need to install all of these if they're already in your backend venv
# Try installing just the ones that are missing

