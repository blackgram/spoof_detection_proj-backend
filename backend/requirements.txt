# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Image processing (for reference image compression during onboarding)
Pillow>=10.3.0

# HTTP client (for liveliness service calls)
httpx>=0.26.0

# Pydantic for data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# FIDO2 / WebAuthn (Passkey) server
fido2>=2.0.0

# Device-bound key auth (Ed25519 verify)
cryptography>=42.0.0

# Firestore (Google Cloud)
google-cloud-firestore>=2.16.0

# Logging
python-json-logger>=2.0.7

